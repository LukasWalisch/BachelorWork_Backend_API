import express from 'express';
import jwt from 'jwt-simple';

let router = express.Router();

router.post("/login",(res,req)=>{

});

router.get("/patterns",(res,req)=>{

})

router.get("/patterns/:pattern_id",(res,req)=>{

})



export default router;
